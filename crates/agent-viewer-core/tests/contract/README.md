# Contract test modules

Files in this directory are modules included by future Rust test binaries that
live directly under `tests`. Cargo automatically compiles Rust files
directly under `tests`, so nothing in this directory is a test target
on its own.

This directory intentionally contains no Rust module yet.
